# DATA 260 - shared dependencies
# Python 3.11 or 3.12 required. Python 3.13 breaks langchain's numpy dependency.

# HW1 Part 2 - Planner / Reviewer / Finalizer agent pipeline
langchain-core>=0.3
langchain-ollama>=0.2
langchain-community>=0.3

# HW1 Part 4 - model adapter with token accounting
ollama>=0.4

# HW2 Part 2 - FastAPI backend on PORT_BASE 8275
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27          # FastAPI TestClient, used by verify_hw02.py

# HW2 Parts 3 and 4 - stateful agent graph and output-schema validation
langgraph>=0.2
pydantic>=2.6

# shared
python-dotenv>=1.0
requests>=2.31

# HW3 Part 1 - auth app (Jinja2 templates, form posts, signed session cookie, bcrypt)
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
bcrypt>=4.0

# HW3 Part 2 - chunking comparison
llama-index-core>=0.12
llama-index-embeddings-huggingface>=0.5
sentence-transformers>=3.0
faiss-cpu>=1.8       # listed by the assignment; the graded run uses SimpleVectorStore
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
beautifulsoup4>=4.12
lxml>=5.0            # XML parser for the eCFR download
